// next.config.mjs

/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "images.unsplash.com", "i.pravatar.cc", "randomuser.me"],
  },
};

export default nextConfig;
